\section{Type Slicing in Hazel by Example}
\label{sec:slicing-by-example}

{This section previews type slicing, formally developed in \cref{sec:synthesis,sec:analysis} over the core calculus of \cref{sec:core-calculus}, through two worked examples in the Hazel implementation. Type slicing answers with a \synsrc{synthesis} slice explains why the term \emph{has} its type and an \anasrc{analysis} slice explains why its context \emph{demands} one. Hazel renders each answer by folding away every fragment the queried type does not require.}

\tikzset{
  cobase/.style={font=\scriptsize\sffamily, align=left, inner sep=2pt,
    rounded corners=1.5pt, fill=white, fill opacity=0.9, text opacity=1,
    line width=0.5pt},
  synco/.style={cobase, draw=NavyBlue, text=NavyBlue!75!black},
  anaco/.style={cobase, draw=OliveGreen!70!black, text=OliveGreen!45!black},
  foldco/.style={cobase, draw=black!45, text=black!55},
  errco/.style={cobase, draw=BrickRed, text=BrickRed!85!black},
  synl/.style={-{Stealth[length=1.5mm]}, line width=0.5pt, NavyBlue},
  anal/.style={-{Stealth[length=1.5mm]}, line width=0.5pt, OliveGreen!70!black},
  foldl/.style={-{Stealth[length=1.5mm]}, line width=0.5pt, black!45},
  errl/.style={-{Stealth[length=1.5mm]}, line width=0.5pt, BrickRed},
}
\newcommand{\foldarrow}[1]{\par\vspace{0.7mm}%
  {\footnotesize\sffamily\color{black!55}$\big\Downarrow$\ \ #1}\par\vspace{0.7mm}}

\begin{figure}[t]
\centering

\begin{tikzpicture}
\node[anchor=south west, inner sep=0] (img) at (0,0)
  {\includegraphics[width=0.9\linewidth]{media/syn-parse-digit.png}};
\draw[black!35, line width=0.35pt] (img.south west) rectangle (img.north east);
\begin{scope}[x={(img.south east)},y={(img.north west)}]
  \node[synco] (a1) at (0.72,0.205) {select \texttt{parse\_digit}};
  \draw[synl] (a1.west) -- (0.155,0.205);
  \node[synco] (a2) at (0.75,0.125) {synthesised type};
  \draw[synl] (a2) -- (0.52,0.05);
\end{scope}
\end{tikzpicture}

\foldarrow{fold to the synthesis slice}

\begin{tikzpicture}
\node[anchor=south west, inner sep=0] (img) at (0,0)
  {\includegraphics[width=0.9\linewidth]{media/synslice-parse-digit.png}};
\draw[black!35, line width=0.35pt] (img.south west) rectangle (img.north east);
\end{tikzpicture}

\caption{Synthesis slicing for \synsrc{\texttt{parse\_digit}}. Selecting the
reference and querying its synthesised type \synsrc{\texttt{String → Option
Digit}} folds the function to the fragments that \emph{source} that type.}
\label{fig:parse-digit-slices}
\label{fig:parse-digit-synthesis}
\end{figure}

\Cref{fig:parse-digit-synthesis} slice a small character parser. The synthesis slice folds the whole function to just the \texttt{String} domain annotation, and the first branch determining the result option type, discarding all other branches.

Queries may target \emph{part} of a reported type: asking only about the domain \synsrc{\texttt{String → \fold}} shrinks the slice to exclude the entire function body. This is the minimal \textit{analysis} slice which enforces \anasrc{\texttt{String}} on the argument (\cref{fig:parse-digit-analysis}).
\begin{figure}[h]
\centering

\begin{subfigure}[t]{0.49\linewidth}
\centering
\begin{tikzpicture}
\node[anchor=south west, inner sep=0] (img) at (0,0)
  {\includegraphics[width=\linewidth, trim={0 28pt 170pt 0}, clip]{media/synslice-parse-digit-no-result.png}};
\draw[black!35, line width=0.35pt] (img.south west) rectangle (img.north east);
\begin{scope}[x={(img.south east)},y={(img.north west)}]
  \node[synco] (d1) at (0.66,0.31) {result type folded};
  \draw[synl] (d1.south east) -- (0.78,0.06);
\end{scope}
\end{tikzpicture}
\caption{Querying only the domain of \synsrc{\texttt{parse\_digit}}.}
\label{fig:parse-digit-domain-query}
\end{subfigure}\hfill%
\begin{subfigure}[t]{0.49\linewidth}
\centering
\begin{tikzpicture}
\node[anchor=south west, inner sep=0] (img) at (0,0)
  {\includegraphics[width=\linewidth]{media/anaslice-arg.png}};
\draw[black!35, line width=0.35pt] (img.south west) rectangle (img.north east);
\begin{scope}[x={(img.south east)},y={(img.north west)}]
  \node[anaco] (c1) at (0.80,0.42) {selected argument};
  \draw[anal] (c1.west) -- (0.30,0.42);
  \draw[anal, {Stealth[length=1.5mm]}-, out=-90, in=-90, looseness=1.4]
    (0.57,0.585) to (0.30,0.44);
  \node[anaco] (c2) at (0.72,0.66) {expected \texttt{String}\\ from the annotation};
  \draw[anal] (c2.west) -- (0.575,0.60);
\end{scope}
\end{tikzpicture}
\caption{Explaining the expected type of the argument.}
\label{fig:parse-digit-argument-analysis}
\end{subfigure}

\caption{Partial synthesis and analysis slicing for \synsrc{\texttt{parse\_digit}}.
Querying only the function domain folds the result type; selecting the argument
and querying why it must be a \anasrc{\texttt{String}} keeps only the application
and the domain annotation.}
\label{fig:parse-digit-analysis}
\end{figure}
\begin{figure}[t]
\centering

\begin{tikzpicture}
\node[anchor=south west, inner sep=0] (img) at (0,0)
  {\includegraphics[width=0.98\linewidth]{media/extended-error.png}};
\draw[black!35, line width=0.35pt] (img.south west) rectangle (img.north east);
\begin{scope}[x={(img.south east)},y={(img.north west)}]
  \node[errco] (e1) at (0.83,0.32) {selected literal synthesises\\
    \texttt{Digit → String → Option(\ldots)}};
  \draw[errl] (e1) -- (0.575,0.195);
  \node[anaco] (e2) at (0.45,0.135) {\ldots but its position demands\\
    \texttt{Digit → Option((String, Pin))}};
  \draw[anal] (e2) -- (0.66,0.035);
  \node[errco] (e3) at (0.77,0.135) {press \emph{Explain error}};
  \draw[errl] (e3) -- (0.925,0.035);
\end{scope}
\end{tikzpicture}

\foldarrow{explain the error}

\begin{tikzpicture}
\node[anchor=south west, inner sep=0] (img) at (0,0)
  {\includegraphics[width=0.88\linewidth]{media/extended-explained-error.png}};
\draw[black!35, line width=0.35pt] (img.south west) rectangle (img.north east);
\begin{scope}[x={(img.south east)},y={(img.north west)}]
  \node[anaco] (f1) at (0.68,0.64) {source of the expected type:\\
    \texttt{seq}'s annotation \texttt{A → Option((String, B))}};
  \draw[anal] (f1.west) -- (0.36,0.585);
  \node[errco] (f2) at (0.60,0.245) {the error: a mis-shaped literal};
  \draw[errl] (f2.west) -- (0.37,0.245);
  \node[foldco] (f3) at (0.70,0.43) {everything else\\folds away};
  \draw[foldl] (f3.west) -- (0.52,0.45);
\end{scope}
\end{tikzpicture}

\caption{Explaining a type error in a twenty-line parser combinator. The inner
function literal is marked because its synthesised \synsrc{\texttt{Digit → String →
Option(\ldots)}} is inconsistent with the \anasrc{\texttt{Digit → Option((String,
Pin))}} its position demands. \emph{Explain error} collapses the program to the
offending literal and the annotation on \texttt{seq} that is the source of the
expectation.}
\label{fig:explain-error}
\end{figure}

{\Cref{fig:explain-error} considers a much more complex error where a sequencing parser combinator has been defined incorrectly, but used correctly. As is usual, the error has been incorrectly localised to the usage site, trusting the (incorrect) annotation. Specifically, \texttt{seq}'s continuation is annotated as if it returns an \texttt{Option} immediately, rather than first accepting the remaining \texttt{String} threaded out of the previous parser. The use site provides the intended parser continuation.

\emph{Explain error} queries the slices by taking the outermost inconsistency in the types (the function vs the option in the return type). The resulting slice collapses the program to include only the function shape of the continuation and the offending annotation.

Crucially, the slice did not assume the annotation is ground truth. It shows the user why both the expectation's \emph{source} (the annotation) and the \emph{usage} (the function), leaving the programmer to judge which is wrong. This is justified as programmers often add annotations incorrectly to existing code (49\% of the time~\cite{TypeAnnotationStudy}).} Should the user instead wish to trust the context unilaterally, they can simply only consider the synthesis slice (ignoring the annotations). \Cref{sec:marking} makes this precise, applying slicing to marked ill-typed programs.

{The user interface is relatively primitive, only folding sliced-away code. Further directions are considered in \cref{sec:future-ui}, such as highlighting \textit{sources} of type information (i.e. the annotation) in order to distinguish these from the `spine' of the program around the selected term (required only to make it syntactically well-formed).}

%%% Local Variables:
%%% mode: LaTeX
%%% TeX-master: "main"
%%% End:
